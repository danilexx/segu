import React, { Fragment } from "react";
import BigScreenMenu from "../BigScreenMenu";

const Menu = () => {
  return (
    <Fragment>
      <BigScreenMenu />
    </Fragment>
  );
};

export default Menu;
